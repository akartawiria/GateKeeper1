@page "/image"

<h1>Dynamic Image Source Example</h1>

@*<div class="container mt-3">
  <h2>Aligning images</h2>
  <p>Use the float classes to float the image to the left or to the right:</p> 
  <img src="@imageSource" class="float-start" alt="Paris" width="304" height="236"> 
  <img src="@imageSource" class="float-end" alt="Paris" width="304" height="236"> 

  <p>
    @foreach (var imageId in  Enumerable.Range(1,3))
            {  
                <button @onclick="@(v => ShowImage(imageId))">
                    Image @imageId
                </button>
            }
            </p>
</div>*@

  

<div class="container-fluid">
    
    <div class="row">
        
            @if (imageSource is not null)
            {
                <div class="col-md-6">
                    <img src="@imageSource" alt="Telkomsel" class="card-img"   />
                </div>
                <div class="col-md-6">
                  <img src="@imageSource" alt="Telkomsel" class="card-img"   /> 
                </div>
            }
        
    </div>

 @*   <br />
    <br />
    *@
    <div class="row">
        <div class="col">
            @foreach (var imageId in  Enumerable.Range(1,3))
            {  
                <button @onclick="() => ShowImage(imageId)">
                    @*@(x =>  imageSource = $"images/image{imageId}.png")*@
                    Image @imageId
                </button>
            }
        </div>
    </div>

</div>

@*@for (var i = 1; i <= 3; i++)v
{
    var imageId = i;
    <button @onclick="() => ShowImage(imageId)">
        Image @imageId
    </button>
}*@

@if (str == null)
{
<p> Loading .......... </p>
}
else
{

    @foreach (var item in str)
    {
        <p>@((MarkupString)item)</p>
    }
}

@code {
    List<string>? str;

    protected override void OnInitialized()
    {
        str = new()
        {
            "1",
            "2",
            "<b>3</b>",
            "3"
        };
    }
    private string? imageSource;

    private void ShowImage(int idM)
    {
        imageSource = $"images/image{idM}.png";
    }
}